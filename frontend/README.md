# Frontend

React + TypeScript + Vite interface for the AI Estimator Platform.

## Setup

```bash
npm install
npm run dev
```

Create `.env` from `.env.example` when needed:

```bash
VITE_API_BASE_URL=http://localhost:8000
```
